{% extends "templates/directory-listing.njk" %}
{% block directory_list %}
    {% for species in collections.aliens %}
        {% if not species.data.hidden %}
            <li>
                {% if species.data.playable %}📑{% else %}📃{% endif %}
                <a href="{{ species.data.permalink | url }}">{{ species.data.species_name }}</a>
            </li>
        {% endif %}
    {% endfor %}
{% endblock %}